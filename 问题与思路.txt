你需要：整理下列任务的思路，并且制定完成他们的先后顺序，而后逐步完成这些改进。
上一轮的尾巴：
1、现在LSP能正常识别了，但是环境中心现在居然报：The installed package inventory for runtime python:3.10 is exact, but the project has 。。。的警告，即使我全部是在python3.10装的库，运行时也是3.10，这个警告的判定逻辑有误。并且环境中心只是识别到了“安装的缓存”，并没识别到当前文件所需的缓存：依赖文件 0 未识别到依赖文件，但是我的项目明明是import numpy as np
import matplotlib.pyplot as plt
from matplotlib.animation import FuncAnimation，这显然是有依赖的，你需要进行修正。

2、服务器选项中的缓存页中显示的当前项目的缓存信息：Pythonpython:3.10摘要c50f503dfc42无依赖文件，可是我明确安装了numpy和matplotlib，环境中心也检测到了已安装依赖有这些库。并且服务器缓存页中显示的缓存大小为141MB，这几乎就是numpy和matplotlib库和配生库的总大小，这说明服务器缓存页其实读到了缓存，但是没有对第三方库进行识别，这种不统一的现象应当剔除，要不做成一个整体的接口供两处调用以确保统一性也行，我需要你修改代码，使得用户可以看到项目下显示的所有库，并可以针对每个库进行独立的删除。

用户端UI设计优化：
1、在刚打开APP时前1s点击“打开文件夹”大按钮无效
2、侧边栏的“快速搜索文件”的模式不对，侧边栏就应该是侧边栏，不应该把查找功能放在正中间，应该给这个快速搜索文件的侧按键做一个侧边栏，功能和文件的检索放在侧边栏，并加入一些建议选项与历史用户选择过的选项。且当前快速搜索文件的选项栏中很多英语指令未被翻译为中文与日语、需要补全这一部分的翻译。
3、同样的，
4、管理员登陆时，从右下角账户区的“管理面板”进入后默认为空页，这是错误的，应该默认打开管理页的“用户”页
5、管理面板中的“用户”页的布局不合适，因为长度不够，所以一次无法显示一个账户的所有操作，但是修复时应当利用下拉菜单重构布局而不是暴力的扩宽页面长度。并且应当增加功能：“查看账户密码”、"设置Quota"
6、“服务器项目”面板中显示的服务器中保存的本账户的项目，有的账户名还是会显示类似pqwvdum的哈希乱码（这个案例在root账号下（密码admin123）你可以自己登陆去测试），这是错误的，给人展示的项目名应该是文件夹的自然语言文字的名字
7、终端下边栏的显示需要优化，不要在多处提醒现在终端的状态，云终端状态采用现有的提示栏进行改造吧：现有提示栏是”云端终端使用已同步的工作区。会话中的文件修改会在终端关闭时丢弃；依赖缓存会保留。“这个模块，把他前面的绿灯改成一个指示灯，绿色为通，黄色为加载中，红色为错误，文本也从固定的提示改为状态提示词，并显示必要信息（比如docker的环境信息、基本信息），而后删去终端下边栏下方的状态提示词和终端会弹出的提示词。


